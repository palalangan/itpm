import {View} from 'react-native';
import React from 'react';

const Gap = ({width, height, paddingBottom}) => {
  return <View style={{width: width, height: height, paddingBottom}} />;
};

export default Gap;
